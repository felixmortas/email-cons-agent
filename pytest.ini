[pytest]
asyncio_mode = strict
asyncio_default_fixture_loop_scope = session
addopts = --browser chromium
markers =
    integration: marks tests as integration tests requiring real credentials (deselect with '-m "not integration"')
